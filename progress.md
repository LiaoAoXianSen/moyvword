# Progress

## 2026-07-12
- 启动“适合摸鱼背词的软件能力升级”并行工作流。
- 启动功能审计、横条设置、面板统计和上下文哨兵子任务；主线程负责数据层与集成。
- 已完成：词书删除与进度、SQLite 备份、连续学习与 7 天预测、横条简/全显示；临时数据层测试通过。
- `npm run dist` 与所有 Node 语法检查通过；发布 `moyu-vocab-strip-release/摸鱼背词横条-v29.exe`。
- 发布后审计完成并修复数据持久化、词本边界、内容安全与位置保存问题；等待 v30 打包。
- `npm run dist` 和最终语法检查通过；发布 `moyu-vocab-strip-release/摸鱼背词横条-v30.exe`。
- 新一轮需求准稿已落盘：单词本只作新词来源，已学词全局复习，今日计划显式选择，新词不自动补。
- 已启动 3 组 AG：上下文哨兵、数据/调度审查、UI/交互审查；主线程开始实现。
- 已完成数据层重构：今日计划只自动加入到期复习，新词需手动/随机加入；不认识/模糊保留在短循环，认识才完成计划项。
- 已完成单词本语义修正：删本只移除归属并保留学习记录；移动仅从当前本移走；学习记录可全局查看。
- 已完成 UI 改造：左侧固定布局、单词本独立管理页、学习记录入口、继续学习弹窗、随机新词预览确认、三档评价和图标资产。
- 语法检查、DOM id 校验、临时 SQLite 行为测试均通过；准备打包 v31。
- 修复左侧栏在主窗口内的拥挤/挤压问题：收窄侧栏、压缩导航与按钮高度，并为侧栏和单词本面板增加滚动兜底。
- `npm run dist` 与所有 Node 语法检查通过；发布 `moyu-vocab-strip-release/摸鱼背词横条-v32.exe`。
- 已启动 v33 主窗口 UI 重设计：仅改视觉层，保持单词本、每日计划与 FSRS 逻辑不变；并行启动结构审计、视觉审计与上下文哨兵。
- 已完成 v33 视觉系统第一轮：中性青蓝主题、紧凑侧栏、整合式统计条、计划区层级强化、减少嵌套边框；结构/视觉/上下文 3 组 AG 审计完成且未改动源码。
- 已完成 v33 验证与发布：`node --check` 通过，`npm start` 正常启动退出，`npm run dist` 成功生成便携版 `dist/摸鱼背词横条-v33.exe`。
- v34 单词本管理调整：侧栏收敛为当前单词本摘要与管理入口；管理页首先显示当前本，并提供切换、导入、下载、重命名、备份与删除操作，其他词本仅作为切换列表。
- v34 侧栏与词库页再整理：移除侧栏当前单词本面板、导入词书、下载词书和横条开关；侧栏只保留首页、当前本单词、学习记录、单词本管理和底部设置。
- v34 修复导航双高亮：主导航统一为 `data-nav` 单一 active key；错词、收藏、已掌握、短循环、到期、普通到期、顽固、短循环到期改为词库页内筛选按钮。
- v34 横条开关移入设置弹窗；单词本导入/下载只保留在单词本管理页；补充筛选刷新防乱序保护。
- v34 验证：`node --check src/app.js src/main.js src/store.js src/preload.js` 通过，DOM id 引用校验通过，`npm run dist` 成功生成 `dist/摸鱼背词横条-v34.exe`。
- 已将新版 `dist/摸鱼背词横条-v34.exe` 同步覆盖到外层发布目录 `moyu-vocab-strip-release/摸鱼背词横条-v34.exe`，避免误用旧发布包。
- v34 词库筛选简化：用户可见筛选只保留全部、待复习、错词、收藏、已掌握；普通到期、顽固、短循环到期等内部调度概念不再常驻显示。
- v34 文案简化：将复习负债改为待复习池，首页债务行改为重点复习、普通复习、短期复习；重新通过 `node --check src/app.js`、DOM id 校验，并重新打包同步 `moyu-vocab-strip-release/摸鱼背词横条-v34.exe`。
- v35 修复随机新词数量默认值：词库页和继续学习弹窗的随机新词数量不再写死 10，改为按每日目标、今日已完成、当前计划占用和可用新词自动建议；例如每日目标 50 且已完成 1 个复习时默认 49。
- v35 打包：因外层 `moyu-vocab-strip-release/摸鱼背词横条-v34.exe` 正在运行被占用，已将 `package.json` artifactName 升为 v35，生成并复制 `moyu-vocab-strip-release/摸鱼背词横条-v35.exe`。

## 2026-07-13
- v42 已完成自动发音、在线 MP3 回退与设置内音量滑杆；开始实现用户新增的 Alt+W 快速搜词工作流。
- 搜词方案：优先返回当前词本命中项；没有命中时请求 DictionaryAPI，并将结果转成可直接加入当前词本/今日计划的词条。
- v44 完成墨墨式调度迁移：每日首次评分才更新 FSRS 长期参数；同日短循环只调整重现时间、卡片间隔和优先级；从词库切换单词成为不写入调度的手动回顾。
- v44 新增静态词难度、用户记忆系数缓慢自适应、四档评分（熟知 Alt+V）；到期复习全量进入计划，剩余目标自动填新词，手动/随机新词可替换自动新词。
- 验证：Node 语法检查通过；临时 SQLite 行为测试通过（日内长期字段不变、手动回顾零副作用、超额到期词全量进入计划、手动新词替换）；`npm run dist` 成功生成 `dist/摸鱼背词横条-v44.exe`。
- 已迁移到 `D:\project\moyvword`：复制源码、文档、v44 和 SQLite 主库/备份；新目录 `npm install`、全部语法检查、`npm run dist` 均通过。新构建 EXE 已同步到 `release\摸鱼背词横条-v44.exe`。
- v45 完成维护优化：修复 SQLite 版本持久化导致的自动发音重启失效；搜索输入增加防抖；主数据改为程序目录 `data`，C 盘保留兜底镜像并支持自定义备份目录。
- v45 继续优化：UI 状态延迟保存、日内短循环剩余次数递减、Electron 窗口/CSP 加固、快捷键注册失败提示、横条高度修正和快捷键 DOM 安全渲染。
- v46 修正 portable EXE 临时解包路径导致主数据读到 Temp 的问题：当 EXE 位于 `dist` 或 `release` 时，默认读取项目根目录 `data\moyu-vocab.sqlite`；C 盘恢复为旧版 `moyu-vocab-strip` 兜底镜像路径。
- 启动“今日回顾”功能：用户确认回顾必须在今日计划完成后开启，且回顾中不认识/模糊只增加本次回顾出现次数，不影响任何正式学习进度。
- 今日回顾功能完成：新增 `session.todayReview` 队列、`today-review:start` IPC、首页“复习今日已学”入口；临时 SQLite 场景验证“不认识”只追加 3 张回顾卡且不改长期/短循环字段，Node 语法检查和 `npm run dist` 均通过。

## 2026-07-14
- 已修复手动查看上一词后的返回路径：`src/store.js` 用 `session.manualReturnId` 保存原当前词；“下一个”及评分会优先返回该词，删除词时清理失效 ID。
- 验证通过：`node --check src/store.js`，临时数据场景覆盖“上一词 → 下一个”和“上一词 → 认识”。
- 已升级至 v53 / 0.1.5；`npm run dist` 成功生成 `dist/摸鱼单词v53版本.exe`，并已同步到 `release/摸鱼单词v53版本.exe`。
- 正在修复评分或跳过上一词后的历史链：原因是返回原词时没有将手动查看词恢复到历史栈。
- 已修复并用临时数据验证连续“上一词 → 评分/跳过 → 返回 → 再上一词”顺序；v54 / 0.1.6 的便携版构建成功。
- 发布同步第一次尝试未找到预期的 `dist/摸鱼单词v54版本.exe`；正在定位 Electron Builder 实际写入的 EXE 路径后改用该路径同步。
- 第二次构建尝试因缩写配置参数被解析为配置文件而报 `ENOENT`，尚未开始打包；下一步改查构建器长参数语法后重新输出到发布目录。
- 使用正确的 `--config.directories.output=release` 后构建器仍显示 portable 构建完成，但 EXE 随即缺失；`release/win-unpacked` 完整。已连续出现三次，暂不发布不完整产物，等待安全软件放行。
- v55：彻底拆分浏览导航与学习调度。新增 `src/navigation.js`（trail + index + liveWordId + mode），`fillLearningWindow/getState` 不再改导航；评分后上一词/下一词顺序已用 `scripts/test-navigation.js` 锁定。
- v56 / 0.1.8：恢复点系统上线。`data/snapshots/` 保存每日自动、手动、恢复前快照；设置页可创建/列表/恢复；恢复前二次确认并自动再备份。`scripts/test-snapshots.js` 与导航测试通过；产物 `dist/摸鱼单词v56版本.exe`。今日学习数据未回滚。
- v57 / 0.1.9：修复每日恢复点重复创建（dayKey 去重），收紧保留：每日7/手动8/恢复前5；清理已有同日重复。
- v58 / 0.1.10：横条外观设置上线。设置页可调整横条长度、背景透明度和主文字颜色；主进程实时调整窗口宽度，横条 renderer 通过 CSS 变量应用外观。语法检查、导航/恢复点回归测试通过；`npm run dist` 生成 `dist/摸鱼单词v58版本.exe`。
- v59 / 0.1.11：紧急修复 v58 启动失败，补回 `src/main.js` 中缺失的 `stripAppearance/stripHeight/applyStripAppearance` helper；语法检查、导航/恢复点回归测试通过；`npm run dist` 生成 `dist/摸鱼单词v59版本.exe`。
- v61 / 0.1.13：手动加入今日记忆不再因名额满被拦截，也不再替换已有自动新词；自动新词改为复习完成后按 `计划数 - 复习数 - 手动加入数` 补齐。不认识/模糊后需连续 3 次“认识”才完成计划项，并保留确认 streak。导航回归与临时确认场景测试通过；`npm run dist` 生成 `dist/摸鱼单词v61版本.exe`，并已同步到 `release/摸鱼单词v61版本.exe`。
- v62 / 0.1.14：修复“今日计划还剩 4 个但显示没有单词”——短循环等待词会进入学习窗口，全部卡在 cards-before 时自动解除空转；空状态文案说明在短循环等待。同步 `app.html/main/strip` 左上角与窗口标题到 v62。
- v63 / 0.1.15：对齐墨墨跨天调度。G1 忘记 1-3 天、G2 模糊 3-7 天、G3 认识 7/15/30/60…、G4 熟知永久掌握；仅当日首次正式评分改长期计划；上一词/手动回顾零副作用且不可改判；同日重复只进短循环；昨日高遗忘压缩新词。单元间隔与导航回归通过。
- v64 / 0.1.16：修正“不认识”跨天语义为次日必回（不再 1-3 天单次后推）；模糊 2-3 天；认识阶梯改为 1/3/7/15…；数据版本 9 回补历史 again 词到今天。导航回归与间隔单测通过。
- v65 / 0.1.17：今日计划同词去重；当天已完成词不再重复入计划/待复习池；不认识/模糊后需连续 3 次认识才出计划，中途再不会清零；确认中认识保持短循环。
- v66 / 0.1.18：横条紧凑模式释义不再省略号截断；超长释义在右侧释义框内自动横向滚动，鼠标悬停暂停。
- v67 / 0.1.19：`Alt+W` 查词改为开关（再按关闭）；查词结果明确显示「已背诵/已掌握/今日已加入/未背过」状态标签。

## 2026-07-20
- 开始实现用户确认的新调度规则：长期排期动态计算但不展示；当天多次出现按隐藏今日进度推进；上一词仅允许修正当前词的前一个并可重算长期状态。
- 已完成动态排期与单槽纠错实现：`src/scheduler.js` 改为 FSRS/历史驱动的每词长期间隔；`src/store.js` 新增隐藏 `todayProgress` 与 `ratingCorrection` 快照槽；主界面/横条移除可见“下次复习”文案。
- 验证通过：`node scripts/test-navigation.js`、`node scripts/test-dynamic-scheduling.js`、`node scripts/test-snapshots.js`、全量 `node --check`、`git diff --check`。planning catchup 脚本路径不存在，已跳过该辅助恢复步骤。
