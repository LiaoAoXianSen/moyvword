## 学习窗口调度修复 - 09:58
- 为 moyu-vocab-strip 增加可配置的学习窗口，默认 15，复习优先但不会让顽固错词永久阻塞新词。
- 答对词移出窗口并补入新词；答错词在短循环内重现，未来到期词不会被提前插回。
- 修改了 src/store.js、src/app.html、src/app.js；通过 Node 语法、临时调度场景和 
pm run pack 验证。
## 便携版打包完成 - 10:03
- 已生成 Windows 便携版安装包 moyu-vocab-strip/dist/摸鱼背词横条 0.1.0.exe，大小约 76 MB。
- 更新 package.json：正式打包跳过不可达的 winCodeSign 下载，适用于未签名的便携版本。
- 
pm run dist 成功。
## 发布目录归位 - 10:07
- 确认 moyu-vocab-strip-release 是既有发布目录；源码和打包中间产物位于 moyu-vocab-strip。
- 已将最新便携版复制为 moyu-vocab-strip-release/摸鱼背词横条-v27.exe，与 v22-v26 保持一致。
## 艾宾浩斯学习闭环升级 - 10:22
- src/store.js 新增按当前词本计算的每日计划、记忆阶段分布、短循环与分层复习负债统计；短循环已就绪即使未来时间未到也会纳入待清理。
- src/app.html、src/app.js、src/app.css 增加学习面板的计划进度、10 阶段可视化和可点击的负债筛选入口。
- 临时 SQLite 场景测试与 Node 语法检查通过；已打包发布 moyu-vocab-strip-release/摸鱼背词横条-v28.exe。
- 打包时发现 dist/摸鱼背词横条 0.1.0.exe 正在运行导致同名文件锁定，已改用版本化产物名避免覆盖。
## 横条专注显示控制 - 10:32
- 在 src/strip.html、src/strip.js、src/strip.css 增加持久化的专注显示切换；通过现有 settings:update IPC 保存 stripCompactMode。
- 专注模式隐藏音标并略微放大单词；按钮在“简/全”间切换，具有动态提示和 ria-pressed 状态。
- 已运行 
ode --check .\src\strip.js 与 git diff --check -- src/strip.html src/strip.js src/strip.css，均通过。
- 未改动主进程、preload 或 store；工作区 Git 根包含大量未跟踪发布文件，未触碰。## 适合摸鱼背词的能力升级 v29 - 10:33
- 使用并行代理审计、横条和管理面板实现；代理均已关闭回收，上下文哨兵摘要确认旧打包锁文件链路可忽略。
- 增加词书安全删除与按词本进度，跨本词仅移除归属不丢记忆；默认词本受保护。
- 增加 SQLite 备份、连续学习、最近 7 天回顾、未来 7 天复习预测；学习日志持久化在 SQLite。
- 横条增加持久化简/全显示模式；管理面板增加备份、删除词书、词书进度及预测展示。
- 已验证临时 SQLite 生命周期/备份/重启恢复、全部 Node 语法检查和 
pm run dist；发布 moyu-vocab-strip-release/摸鱼背词横条-v29.exe。
- 范围决定：不做云同步、题库练习、离线音频、词根百科，以保持低打扰桌面横条定位。
## Electron IPC 与生命周期审查 - 16:42
- 对 D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip 完成只读审查，未修改项目源文件。
- 确认三项具体问题：空词库在重启时被种子词重置；SQLite 直接覆写导致中断写入后数据库损坏并使启动停在无窗口状态；横条位置防抖定时器在退出或关闭时丢失最后位置。
- 审查范围为 Electron 单实例、窗口生命周期、store/persistence、备份与 IPC；未发现需要报告的单实例或备份复制逻辑缺陷。
## 摸鱼背词数据与调度审查 - 00:00
- 审查项目 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip`，按用户要求未修改源码。
- 重点阅读 `src/persistence.js`、`src/scheduler.js`、`src/store.js`、`src/app.js`，并运行 `node --check` 验证四个文件语法通过。
- 结论：基础词本、学习记录、今日计划、FSRS/短循环已有实现，但每日新词上限、词本隔离、当前词校验存在关键缺口。
- 发现风险：`chooseNext` 兜底可能绕过学习窗口展示未计划新词；新词首次标记 again/hard 不计入新词进度；历史/批量移动/已掌握词可能让当前词与活跃词本错位并污染学习记录。
## 摸鱼背词 UI交互只读审查 - 11:58
- 用户要求不要编辑文件，只读审查工作目录 D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip。
- 已阅读重点文件 src/app.html、src/app.css、src/app.js、src/strip.html、src/strip.css、src/strip.js，并补查 src/store.js、src/main.js、package.json；detect.mjs --json .\src\app.html 返回空数组。
- 审查缺口集中在左侧导航非固定且未滚动隔离、单词本管理缺少主内容 tab、切换单词本反馈弱且共享词可能不变、全选无法按当前列表再次取消、按钮文案语义弱、今日计划完成后缺少继续学习完成态、三档快捷键与四档按钮不一致、应用图标仅有运行时 W 托盘 SVG。
- 未修改源码；因用户要求不要编辑文件，未写 .impeccable/critique 快照。
## 摸鱼背词 v31 计划重构 - 12:20
- 用户最终确认：单词本只作为新词来源，学习记录按单词全局保存；删除单词本不删除单词和学习记录，已学无本词仍进入复习队列。
- 已在 D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip 实现今日计划：初始只自动加入到期复习，最多每日目标数量；新词必须手动或随机预览确认后加入；跨天未完成计划保留。
- 已修改 src\store.js、src\app.js、src\app.html、src\app.css、src\main.js、src\preload.js、src\scheduler.js、src\persistence.js、package.json，并新增 ssets\icon.svg、ssets\icon.png、ssets\icon.ico。
- 评价档位统一为三档：Alt+Z 不认识、Alt+X 模糊、Alt+C 认识；不认识/模糊留在短循环，认识才完成今日计划项。
- UI 已补左侧固定布局、单词本独立管理页、学习记录入口、继续学习弹窗、随机新词预览确认、全选/取消当前列表。
- 验证通过：
ode --check 关键 JS、DOM id 静态校验、临时 SQLite 行为测试、
pm run dist；产物为 dist\摸鱼背词横条-v31.exe。
## v33 主界面 UI 重设计 - 15:56
- 完成 Electron 主窗口视觉重设计，仅修改 `src/app.css` 与任务记录；保留单词本、每日计划、FSRS 和横条逻辑。
- 新设计使用中性青蓝桌面工作台视觉，侧栏导航和词书选中语义分离，统计区在 1040px 以下收为三列以适配 920px 最小窗口。
- 验证通过：`node --check src/app.js`、`node --check src/main.js`、`node --check src/scheduler.js`、`npm start`、`npm run dist`。
- 发布产物已生成并同步：`dist/摸鱼背词横条-v33.exe`、`../moyu-vocab-strip-release/摸鱼背词横条-v33.exe`，大小 76078439 bytes。
- 忽略旧 Qt 逆向链路与根目录旧版 EXE；当前可维护源码是 `moyu-vocab-strip/src`。
## 创建 Goal 心跳守护 - 15:58
- 为当前线程创建名为“Goal 继续心跳守护”的心跳自动化，每分钟检查一次 active goal。
- 当存在 active goal 且当前任务未推进时，自动向本线程发送“继续”；无 active goal 时不发送。
## v34 单词本管理入口重构 - 16:03
- 侧栏单词本区域改为仅展示当前单词本摘要与“管理单词本”入口，移除窄侧栏内的新建、重命名、备份和删除按钮。
- `src/app.html`、`src/app.js`、`src/app.css`：管理页首屏展示当前本，支持下拉切换、导入、下载、重命名、备份和删除；其他单词本只提供切换。
- 验证通过：`node --check src/app.js`、DOM ID 完整性检查、`npm start`、`npm run dist`。
- 发布：`dist/摸鱼背词横条-v34.exe` 已同步到 `../moyu-vocab-strip-release/摸鱼背词横条-v34.exe`。
## 摸鱼背词 v34 侧栏与词库页整理 - 16:36
- 本轮目标：整理 `moyu-vocab-strip` 主界面，修复导航双高亮，移除侧栏重复的单词本/导入/下载/横条控制。
- 已修改 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.html`：侧栏只保留首页、当前本单词、学习记录、单词本管理和底部设置；错词/收藏/已掌握/短循环/到期等移动到词库页筛选；横条开关移动到设置。
- 已修改 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.js`：主导航改为单一 `data-nav` active key，词库筛选独立为 `data-word-filter`；导入/下载由单词本管理页直接触发；增加 `loopDue` 筛选和 `refreshWords` 乱序保护。
- 已修改 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.css`：移除旧侧栏词本面板样式，重做侧栏底部设置、词库筛选 chips、设置中的横条区域，并降低二级筛选视觉层级。
- 已更新 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\task_plan.md` 和 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\progress.md` 记录 v34 工作。
- 验证：`node --check src/app.js src/main.js src/store.js src/preload.js` 通过；DOM id 引用校验通过；`npm run dist` 成功。
- 产物：生成 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\dist\摸鱼背词横条-v34.exe`，并同步覆盖到 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip-release\摸鱼背词横条-v34.exe`。
- 注意：当前外层 git 工作树中这些源码/发布文件多为未跟踪状态；未创建提交。## 摸鱼背词 v34 词库筛选简化 - 16:50
- 用户指出 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.html` 词库筛选按钮过多，对用户无用。
- 已将词库页可见筛选收敛为全部、待复习、错词、收藏、已掌握；普通到期、顽固、短循环到期、短循环等内部调度状态不再常驻显示。
- 已将首页文案从复习负债改为待复习池，债务分组改为重点复习、普通复习、短期复习；`D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.js` 的标题标签同步改为待复习、重点复习、普通复习。
- 验证：`node --check src/app.js` 通过，DOM id 引用校验通过，`npm run dist` 成功。
- 产物：重新生成 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\dist\摸鱼背词横条-v34.exe`，并同步覆盖 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip-release\摸鱼背词横条-v34.exe`。## 摸鱼背词 v35 随机新词数量联动每日目标 - 17:03
- 用户发现设置 `每日目标=50`，但 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.html` 词库页随机挑选数量仍显示 10。
- 原因：`managerRandomCount` 和 `randomNewCount` 的 HTML 默认值此前写死为 10，未与设置里的每日目标联动。
- 已修改 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.js`：新增 `suggestedNewWordCount(plan)`，按每日目标、今日已完成、当前计划占用和可用新词计算建议数量；例如每日目标 50 且已完成 1 个复习时默认 49。
- 已将 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\src\app.html` 中两个随机新词输入初始值改为 50。
- 验证：`node --check src/app.js src/main.js src/store.js src/preload.js` 通过；DOM id 引用校验通过；`npm run dist` 成功。
- 因外层 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip-release\摸鱼背词横条-v34.exe` 正在运行被占用，未覆盖 v34；已将 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\package.json` artifactName 升为 v35，并发布 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip-release\摸鱼背词横条-v35.exe`。