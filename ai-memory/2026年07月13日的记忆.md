## v40 自主选词与词库分页 - 10:10
- 在 moyu-vocab-strip/src/app.html、moyu-vocab-strip/src/app.js、moyu-vocab-strip/src/app.css 增加“自主选择”新词入口，与随机挑选位于同一今日计划弹窗，可搜索、勾选并加入计划。
- 词库左栏改为首字母定位（A-Z / #）和每页 50 条的分页加载；moyu-vocab-strip/src/store.js 的 listWords 只返回当前页数据，避免整本词库一次性传入渲染进程。
- 修正词库左栏的固定高度与内部滚动，搜索框、字母索引和分页控件保持可见。
- 已执行 
ode --check src/app.js、
ode --check src/store.js 和 
pm run dist；生成 moyu-vocab-strip/dist/摸鱼背词横条-v40.exe，SHA256 E8E4DDE521ABBCD923F87E32923798F58B6B5D28280C795B34D0689ADF5CFBA3。## 排查 Alt+C 全局快捷键 - 10:13
- 确认 Alt+C 在 moyu-vocab-strip/src/main.js 的全局快捷键表中绑定为 ate-good（认识）；由 globalShortcut.register 注册，应用在后台时也会占用。
- moyu-vocab-strip/src/strip.html 和 moyu-vocab-strip/src/store.js 同步展示其含义为“认识”，不是第二个独立注册来源。
- 本次仅诊断，未改动快捷键。## 更正 Alt+Z 全局快捷键 - 10:14
- 用户更正需排查的是 Alt+Z；其在 moyu-vocab-strip/src/main.js 中绑定为 ate-again（不会/不认识），并通过 globalShortcut.register 全局注册。
- 本次未改动快捷键。## v41 紧凑首字母与自动滚动加载 - 10:23
- 将 moyu-vocab-strip/src/app.html 词库左栏的 A-Z 按钮墙替换为搜索框旁的紧凑首字母下拉框，保留按首字母定位能力。
- 删除上一页/下一页按钮；moyu-vocab-strip/src/app.js 在词表及自主选词列表滚动接近底部时自动请求下一批 50 个单词。
- moyu-vocab-strip/src/store.js 的分页接口继续只返回当前批次，避免整本词书一次性传入渲染进程。
- 已执行 
ode --check src/app.js、
ode --check src/store.js 和 
pm run dist；生成 moyu-vocab-strip/dist/摸鱼背词横条-v41.exe，SHA256 DCA0FD9040C1497A0DEC158F18BDFAA1987A773969356F4F72C992BC9A089968。## 解释短循环等待规则 - 10:42
- 向用户说明当前机制：点“不认识”后，若能继续穿插 2 个其他词会提前再次出现；没有足够词可穿插才需等满 3 分钟。用户表示之前说明未理解，后续应使用具体示例解释。
- 快速搜词与音频设置代码已在工作区修改但尚未完成最终验证/打包；短循环改动尚未实施，等待用户确认期望行为。## 解释短循环穿插实现 - 10:44
- 向用户说明当前实现细节：ateWord 对“不认识”写入 status=learning、due=当前时间+3分钟、loopCardsLeft=2；每次给其他词评分时 	ickSmallLoops 将该计数减一；计数归零时 isLoopReady 允许它在到期前再次进入 chooseNext 优先队列。
- 明确“背两个词”实际指完成两次其他词的评分操作，而非仅浏览两张词卡。## 确认短循环留存语义 - 10:45
- 向用户确认：对计划内单词连续选择“不认识”或“模糊”时，它保持在短循环/今日背诵列表，不会完成或移除；只有选择“认识”才会完成当次计划并按后续间隔安排复习。## 确认今日计划完成条件 - 10:46
- 向用户确认：今日计划完成表示计划内每个词都已选择“认识”并完成当前轮；持续“不认识/模糊”的短循环词仍会留在计划内，因此计划不算完成。## 背词调度模型对照研究 - 10:56
- 用户提供墨墨式 SSP-MMC 调度规范，要求研究与当前应用的差异，未修改业务代码。
- 当前项目长期调度实际使用 	s-fsrs；核心字段为 stability、difficulty、due、lapseCount，实现位于 moyu-vocab-strip/src/scheduler.js。
- 发现当前 ate() 会对同一天的每次重复出场都调用 ateWord()，因此重复巩固会修改长期记忆参数，和用户规范的“当天二次及以上只调场内优先级”冲突。
- 已有计划项确认规则：当天模糊/不认识后，需连续两次认识才完成；实现位于 moyu-vocab-strip/src/store.js，待将长期参数更新与场内反馈拆分后再整体校准。
- 建议后续以“每日首次长期评分 + 当日短循环评分 + 手动回顾不计分”三类事件重构，保留 FSRS 作为长期引擎。
## 墨墨调度只读审计 - 11:02
- 已只读审计 src/store.js 与 src/scheduler.js；未修改项目源码。
- 关键冲突：ate() 无条件调用 ateWord()，当天短循环与通过 setCurrentWord() 进入的手动查看都会更新 FSRS 长期字段。
- 计划层当前只将到期复习按 dailyNew 容量截断，且不自动填充新词；四级评分中的 asy 被折叠为 good。
- 建议后续以评分来源/日内首次标志分流长期调度、日内队列和手动回顾，并补相应场景测试。
## 墨墨式记忆调度 v44 完成 - 11:23
- 完成 `moyu-vocab-strip` 调度迁移：每日首次评分才更新 FSRS 长期字段；同日短循环仅调整 `dayLoop*` 元数据；从词库切换单词为不写入记忆参数的手动回顾。
- 新增静态 `wordDifficulty`、自适应 `userMemoryCoeff`、四档评分（熟知 `Alt+V`）；到期复习不受每日目标截断，剩余容量自动填新词。
- 保留自主选词：手动/随机新词会优先把已自动计划的同词标记为手动；名额满时可替换自动新词，不会挤掉到期复习。
- 改动文件：`src/store.js`、`src/scheduler.js`、`src/main.js`、`src/app.html`、`src/strip.html`、`src/strip.css`、`package.json`、`task_plan.md`、`progress.md`。
- 验证通过：Node 语法检查；临时 SQLite 场景测试覆盖日内评分长期字段不变、手动回顾零副作用、超额到期词全量入计划、手动新词替换；`npm run dist` 成功。
- 发布产物：`D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip\dist\摸鱼背词横条-v44.exe`。## moyvword 迁移前盘点 - 11:49
- 用户要求仅做 `moyvword` 迁移前盘点，不移动、不删除、不修改文件。
- 主工程为 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip`；最终候选程序为其 `dist\摸鱼背词横条-v44.exe`。
- 真实用户背词数据位于 `C:\Users\lihao\AppData\Roaming\moyu-vocab-strip\moyu-vocab.sqlite` 与 `moyu-vocab.sqlite.bak`，迁移时应一并备份；旧 JSON 和缓存不必作为主数据迁移。
- 外层目录与 `moyu-vocab-strip-release` 中有大量 v22-v43 和旧版 EXE，可作为清理候选；未执行清理。`node_modules`、`dist\win-unpacked`、`.tmp-*`、构建调试 YAML 也可在新项目中不迁移。
- 等待用户明确 `moyvword` 的目标绝对路径后，再执行复制校验、数据备份和清理确认单。## D 盘项目目录摸鱼单词版本盘点 - 11:54
- 只读扫描 `D:\project` 后发现一套额外相关版本：`D:\project\moyvdanci\摸鱼单词Release`。
- 该目录是旧 Qt 运行包，包含 `摸鱼单词.exe`、Qt DLL、`treasure.bin`、缓存与模板；没有发现源码文件或项目文档。
- 当前 Electron 项目与 v44 不在该目录；仍位于 `D:\software\摸鱼单词免安装版Release\摸鱼单词Release\moyu-vocab-strip`。
- 未移动或删除任何文件；`D:\project\ai-memeory` 中的同名条目均为记忆记录，不是应用版本。## 项目文档补全并归位源码目录 - 11:59
- 用户要求文档全部放在代码目录以防后续上下文丢失。
- 已在 `moyu-vocab-strip` 根目录更新 `README.md`，新增 `PROJECT_HANDOFF.md`，并将 `task_plan.md` 的 v44 调度阶段标为完成、明确旧规则已废弃。
- `PROJECT_HANDOFF.md` 记录架构职责、v44 调度不变量、运行时 SQLite 路径、构建发布、已验证场景、迁移到 `moyvword` 顺序和不可误删文件。
- 检查外层 `docs` 后未发现项目文档；当前项目文档均位于源码目录，未移动或删除其他内容。## 文档详细性审查与补全 - 12:02
- 用户要求对项目文档做详细性审查并补齐，以便后续无上下文维护。
- 已将 `PROJECT_HANDOFF.md` 扩展到约 12.7 KB，新增：窗口和 IPC 导航、词本边界、SQLite 表/元数据、原子写入与恢复不变量、字段字典、评分与计划状态机、外部能力、测试清单、已知限制和修改风险。
- 已更新 `README.md` 链接；`task_plan.md` 现明确旧“当前准则”为历史记录，并标记快速搜词阶段完成，当前真相以 v44 和 `PROJECT_HANDOFF.md` 为准。
- 文档关键字段与源码做过字符串对照检查；未修改业务代码或发布产物。## Moyvword 项目迁移完成 - 12:10
- 用户授权执行迁移；已将项目复制到 `D:\project\moyvword`，原 `moyu-vocab-strip` 保留为回滚，未清理任何历史 EXE。
- 新项目包含 `src`、`assets`、项目文档、`release\摸鱼背词横条-v44.exe`、`data\moyu-vocab.sqlite`、`data\moyu-vocab.sqlite.bak` 和旧 JSON 归档。
- 目标项目 npm 名已改为 `moyvword`；保留 `productName`、`appId` 和 Electron 用户数据路径以避免已有学习数据变为空。
- 已对照哈希验证复制后的 v44、SQLite 主库和 `.bak`；在 `D:\project\moyvword` 执行 `npm install`、Node 语法检查和 `npm run dist` 均成功，新构建 EXE 已写入 `release`。
- 新增 `MIGRATION.md`，记录数据恢复与清理旧目录前的验收。下一步若用户明确要求，可删除源目录中的历史失败 EXE、旧 release 目录、临时构建目录和旧 Qt 版本；当前均未删除。