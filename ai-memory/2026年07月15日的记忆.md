## moyvword 新词自动入计划逻辑诊断 - 17:51
- 用户明确规则：复习可以自动进入今日计划，但新词必须用户主动添加；即使今日目标 `100`、复习 `30`，剩余 `70` 也只是手动添加额度，不能自动塞新词。
- 诊断发现自动新词来源在 `D:\project\moyvword\src\store.js` 的 `fillAutomaticNewPlan()`，其 `automatic_new` 逻辑会在复习项结束后按 `dailyNew - reviewCount - manualNewCount` 自动加入新词。
- `git blame` 显示该逻辑主要来自 `2356285 fix: add manual words without replacing plan items`，后续 `f2a291e feat: adopt Momo-style cross-day scheduling` 又加入按昨日遗忘率缩放自动新词量。
- 根因是把 `dailyNew`/今日目标理解成需要自动填满的总计划量；正确语义应是复习自动、新词只通过 `addNewWordsToPlan()` 等显式手动入口进入计划。
## moyvword 新词必须手动入计划实现 - 17:56
- 按用户确认的规则修改 `D:\project\moyvword\src\store.js`：复习仍自动进入今日计划，新词即使有目标剩余额度也只能通过显式用户操作进入计划。
- 将原 `fillAutomaticNewPlan()` 替换为 `cleanupAutomaticNewPlan()`，删除按 `dailyNew - reviewCount - manualNewCount` 自动生成 `automatic_new` 计划项的逻辑。
- 新逻辑会清理当天尚未开始学习的旧 `automatic_new` 新词计划项；已经完成或已有学习记录的自动项保留，避免丢失进度。
- 已验证 `node --check src/store.js`、临时数据库场景：目标 `100` 不会自动加新词，手动加词可进入计划，模拟 `automatic_new` 会被清理；并执行 `git diff --check -- src/store.js`。
## moyvword 横条详情滚动显示 - 17:59
- 用户要求横条详情在条框里展示不开时滚动显示；按产品型 UI 局部修复处理。
- 修改 `D:\project\moyvword\src\strip.html`，为横条详情正文新增 `.details-scroll` 包裹 `meta`、释义和例句，评分按钮保留在正文外。
- 修改 `D:\project\moyvword\src\strip.css`，展开状态下 `.details` 改为纵向 flex，`.details-scroll` 占据剩余高度并 `overflow-y: auto`，`#meaning`/`#sentence` 改为多行显示和长词换行。
- 已执行 `node --check src/strip.js` 和 `git diff --check -- src/strip.html src/strip.css`；未提交、未打包。
## moyvword 手动新词计划与横条滚动提交打包 - 18:04
- 已提交 `D:\project\moyvword\src\store.js`、`D:\project\moyvword\src\strip.html`、`D:\project\moyvword\src\strip.css`，提交为 `4ec50b9 Fix manual new-word planning and strip details`。
- 变更包括：复习自动进计划但新词只允许手动加入；清理未开始学习的旧 `automatic_new` 计划项；横条详情正文区域展示不开时局部滚动，评分按钮固定在下方。
- 已执行 `node --check src/store.js`、`node --check src/strip.js`、`git diff --cached --check`/`git diff --check`、`npm run dist`。
- 重新打包产物为 `D:\project\moyvword\dist\摸鱼单词v63版本.exe`，SHA256 `EA258E583C3C85A503CDC81409854891CABDCEB5306BFA42D062F8A8600C9A18`，签名状态 `NotSigned`。
- 旧同名包打包前 SHA256 为 `722B67D34BACA30F985FC8C5FB28762FF0568EA85788217BBA76817E47230E99`；当前未提交项仅剩记忆文件和 `D:\project\moyvword\data\backup-config.json`。
